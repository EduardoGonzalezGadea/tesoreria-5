import { CuentasPersonalesModel } from './cuentas-personales-model';

describe('CuentasPersonalesModel', () => {
  it('should create an instance', () => {
    expect(new CuentasPersonalesModel()).toBeTruthy();
  });
});
